name: simple-cprog
base: core18
version: '1.0.0'
summary: Simple C Prog
description: |
  Simple C Prog
grade: stable
confinement: strict
architectures:
  - build-on: amd64

apps:
  curl:
  run-hello-world:
     command: cprog-v1.0.0/a.out 
  hapis-server:
    command: hapi-server-v1.0.10/hapi-server --logdir="/var/snap/hapi-combo11/current" --metadata_dir="/var/snap/hapi-combo11/current" 

  
parts:
  cprog:
    source: 
    plugin: dump
